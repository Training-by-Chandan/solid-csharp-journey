@model List<Classes>
@{
    Layout = "_Layout";
}

    <div class="row">
        <a href="/class/create" class="btn btn-success">Create</a>
    </div>
    <div class="row">

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Description</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>
                    <a href="/class/edit/@item.Id" class="btn btn-primary">Edit</a>
                    <a href="/class/delete/@item.Id" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
    </div>