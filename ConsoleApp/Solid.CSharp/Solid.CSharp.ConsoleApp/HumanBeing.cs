namespace Solid.DSharp
{
    public class HumanBeing
    {
        //Constructors
        //Variables
        //Properties
        //Methods / Functions

        //Delegates and Events

        //Destructor / Garbage Collector(GC), IDisposable Pattern

        //Constructors
        //1. Special Function
        //2. Runs only once in its lifetime
        //3. Name is same as that of class name
        //4. No return type
        //5. Access modifier : Private (Singleton pattern), protected, public, static (not a access modifier)
        //6. can be overloaded
        public HumanBeing()
        {
        }

        public HumanBeing(int i)
        {
        }

        public HumanBeing(long y)
        {
        }

        public int age = 0;
    }
}